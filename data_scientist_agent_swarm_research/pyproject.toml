[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "fx-research-swarm"
version = "0.1.0"
description = "AI Swarm Data Scientist — autonomous FX prediction model research"
requires-python = ">=3.11"
