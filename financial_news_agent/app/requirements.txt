# News scoring pipeline: Tavily, Novita (OpenAI-compatible), ClickHouse.
# Secrets (API keys, DB credentials) from env only; do not load from file.

tavily-python>=0.5.0
openai>=1.51.2
clickhouse-connect>=0.8.0
PyYAML>=6.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
